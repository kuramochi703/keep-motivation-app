# このアプリのしくみ（小学生でもわかる説明）

> このドキュメントは「AI に指示して開発してきたけど、中身を理解したい」
> という初心者向けに、**お店づくりのたとえ**で全体の仕組みをまとめたものです。

---

## 1. あなたが作ったものは「Webアプリ」

このアプリはブラウザ（Chrome など）で動く **Webアプリ** です。
Webアプリは **家を建てる** とよく似ています。

- **骨組み** → どこに何を置くか
- **壁紙や家具** → 見た目（色・大きさ・丸み）
- **電気や配管** → ボタンを押したときの動き

子どものころに「ブロックで家を作る」経験そのものが Webアプリです。
あなたはすでにそれを 1 つ完成させています。すごいことです。

---

## 2. 使っている技術＝「道具」の紹介

| 道具 | たとえ | ふだん何をしているか |
| --- | --- | --- |
| HTML | 骨組み | 「タイトル」「入力欄」「ボタン」を置く場所を決める |
| CSS | 壁紙と家具 | 色、はみ出す位置、角の丸みなど「見た目」を整える |
| JavaScript | 電気と配管 | ボタンが押せる、画面を切り替える、などの動きを担う |
| TypeScript | JavaScript＋ルール表 | JavaScript に「これは数字」「これは文字」と型を強制したもの |
| React | 部品の工場 | 画面を「パーツ（カード・ボタン）」に分けて組み立てる仕組み |
| Vite | お弁当を詰める器具 | 書いたコードをブラウザが読みやすい形に「詰める」道具 |
| npm | スーパーのレジ係 | 必要な材料（ライブラリ）をダウンロードして管理してくれる |
| Git/GitHub | セーブ機能＋宿題ボックス | 変更履歴をセーブし、チームの共有フォルダにアップロードする |

「技術」といっても、全部 **「この目的のためにある道具」** です。
それぞれ単体ではただの材料で、組み合わせて初めてアプリになります。

---

## 3. あなたのアプリの中身（src フォルダ）

`src/` フォルダの中は **担当者ごとにファイルを分けてある**のが特徴です。

```text
src/
├─ main.tsx        ─ 玄関。最初に全部を呼び出す役
├─ App.tsx         ─ 家の設計図。どの画面を出すか決める
├─ Sidebar.tsx     ─ 横のメニュー（トップ/目標設定/ダッシュボード）
├─ logic.ts        ─ ルールブック（体力の計算・保存のやり方）
├─ useApp.ts       ─ リモコン（今どの画面か、タイマー操作）
├─ pages/          ─ 画面の部品たち
│   ├─ TopPage.tsx      ─ 1枚目：トップ
│   ├─ SetupPage.tsx    ─ 2枚目：目標設定
│   └─ MainPage.tsx     ─ 3枚目：ダッシュボード（メイン画面）
├─ avatar/         ─ アバター（絵と見た目のルール）
└─ ui/             ─ 配色やボタンのデザインルール
```

ポイントは **1 ファイル＝1 つの仕事** ということです。
「どの画面を出すか」「体力の計算」「絵の見た目」が各ファイルに分かれているので、
いじる場所がわかりやすくなっています。

---

## 4. 互いの関係＝「リモコン」のしくみ

アプリの中心に `useApp.ts` という **リモコン** があります。
ここが全画面を操っている司令部です。

```text
ユーザー（あなた）
   ↓ 「はじめる」をクリック
SetupPage（画面）→ 入力を集める
   ↓
useApp.ts（リモコン）→ 「目標設定」から「ダッシュボード」に切り替え
   ↓
MainPage（画面）→ 新しい画面を表示
```

しかも **画面を切り替えても「アバターの体力」という状態は消えません**。
この「状態（state）」は `useApp.ts` がずっと握っていて、
どの画面も **同じデータを共有している**。これが React でアプリを作る
一番重要な考え方です。

---

## 5. データの生き方（ルールブックと日記）

**`logic.ts`（ルールブック）** にはゲームのルールが書いてあります。

- 達成した日は体力 **+12**
- サボった日は体力 **-20**
- 体力が 0〜100 の範囲を超えないようにする

**`localStorage`（日記＝セーブデータ）** は、ブラウザの中にデータを
保存する場所です。「今の体力・目標・名前・達成した日リスト」が記録され、
アプリを閉じても開けば前の続きから始められます。
ゲームのセーブ画面と同じ役割です。

活動の流れはこんな感じです。

```text
ボタンを押す → ルールブック(logic.ts)で計算
           → リモコン(useApp.ts)が状態を更新
           → 画面(MainPage.tsx)が表情を変えて表示
           → 同じ内容を日記(localStorage)に書き込む
```

---

## 6. 「サボるとやつれる」がどう実現されているか

アバターがやつれる瞬間を 1 つ追ってみましょう。

1. あなたが 1 日サボる（達成ボタンを押さない）
2. 次の日にアプリを開く
   → `useApp.ts` が「前回から今日までサボった日がないか」をチェック
3. サボった日が 1 日あった
   → `logic.ts` のルールで **体力を 20 引く**
4. 体力が減る
   → `Avatar.tsx` が「体が小さく、顔色が悪い」絵に切り替える

**「体力という数字」⇔「絵の変化」** がルールブック 1 つでつながっています。
これがこのアプリの心臓部です。

---

## 7. 最後の工程：ビルドと GitHub

普段書いているコードは、ブラウザはそのままでは読めません。
そこで **Vite** が扱いやすい形にまとめます（＝お弁当を詰める）。
これが `npm run build` の仕事です。

- **Git** はセーブ機能。動く状態を「ここからこう変えた」と記録していく
- **GitHub** はそのセーブデータを置く共有ボックス
- **PR（プルリクエスト）** は「改良を本線に取り込んでください」
  という**申請書**

---

## 8. 相互関係を 1 行で

> **HTML・CSS で見た目を作り、React で部品を組み、TypeScript（JS）で
> 動かす。ロジックは logic.ts で計算し、useApp.ts が画面を管理して、
> 保存は localStorage。Vite で詰めて、Git/GitHub で共有する。**

最初の 1 日で、Webアプリの「材料・しくみ・連絡係・保存・共有」まで
全部を通り抜けています。どの部品がどの役目か、ファイル名を見て
「あ、これは絵の担当だ」と思えるようになれば、かなり理解が進んだ証拠です。

---

## 9. 迷ったらここを見る

| 見たいこと | ファイル |
| --- | --- |
| 仕組みの図（Mermaid） | `ARCHITECTURE.md` |
| 画面の構成 | `src/App.tsx` |
| 状態・画面切り替え | `src/useApp.ts` |
| ゲームのルール | `src/logic.ts` |
| 目標設定の画面 | `src/pages/SetupPage.tsx` |
| ダッシュボード | `src/pages/MainPage.tsx` |
| アバターの絵 | `src/avatar/Avatar.tsx` |
| 見た目のデザイン | `src/ui/styles.css` |
