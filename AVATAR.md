# アバター育成システム — 設計とタスク

このアプリの**中心はアバターの育成**です。タイマーも記録も、育成を支えるための部品として位置づけます。
チームの進め方全般は [TEAM.md](./TEAM.md) を参照。

> **2章と3章は決着済みです。**（2026-09-06）
> 2軸に分ける設計を採用し、描き方は **react-three-fiber（three.js）** に決めました。
> 実装は `src/avatar/` に入っています。触り方は
> [`src/avatar/README.md`](./src/avatar/README.md) を読んでください。
> 4章のタスクは、この決定を前提に読み替えてください。

---

## 1. 現状の何が問題か

いまの `logic.ts` には **活力（0〜100）というゲージが1本しかありません。**

```text
初期値 62 ─ 達成すると +12 ─▶ 74 ─▶ 86 ─▶ 98（上限100）
```

**4日続ければ上限に張り付きます。** そこから先はどれだけ続けても見た目は `lv4`（絶好調）のまま変わらない。
つまり今あるのは「今日の体調の表示」であって、育成ではありません。

| 育成に必要なもの | 現状 |
| --- | --- |
| 続けるほど積み上がる | ✕ 4日で頭打ち |
| 積み上げたものが残る | ✕ サボると同じところまで戻る |
| 長く遊べる伸びしろ | ✕ 活力の5段階しかない |

---

## 2. 提案：2軸に分ける

**減らない「成長」と、日々上下する「コンディション」を別々に持ちます。**

```text
軸1  成長ステージ  ← のべ達成日数で決まる。下がらない。   「どこまで育ったか」
軸2  活力 0〜100   ← 既存。やれば+、サボれば−。上下する。 「今の調子はどうか」

           見た目 ＝ ステージ（体格・装備・背景） × 活力（表情・姿勢・色）
```

### 成長ステージ（決定：`src/avatar/stage.ts`）

たまごから孵って育つ7段階にしました。最初の1日で目に見えて変わるのが大事なので、
序盤の刻みを細かくしてあります。

| ステージ | 到達条件（のべ達成日数） | 名前 | 増えるもの |
| --- | --- | --- | --- |
| 0 | 0日 | たまご | まだ殻の中 |
| 1 | 1日 | ひよこ | からだ・あし・くちばし |
| 2 | 3日 | やんちゃひよこ | つばさ |
| 3 | 7日 | もふもふ | しっぽ |
| 4 | 14日 | いっちょまえ | とさか |
| 5 | 30日 | りりしい | マフラー |
| 6 | 60日 | まんまるの主 | 冠 |

### なぜこれがいいか

- **続けた分が消えない** → サボって落ち込んでも、戻ってくれば同じ場所から再開できる
- **伸びしろが長い** → 上限が60日なので、数か月遊べる
- **「サボると、やつれる」は死なない** → ステージは残るが、活力が落ちれば見た目はやつれる。
  *育った達人がやつれている*ほうが、ひよっこがやつれるより効きます

### 決めたこと

1. **ステージは絶対に下がらないか？** → 下がらない。下がると復帰の心理的ハードルが上がるため
2. **区切りの日数は妥当か？** → 上の表で走らせてみて、飽きが来るようなら `stage.ts` の `min` を調整する
3. **ステージが上がると見た目の何が変わるか？** → 上の表の「増えるもの」。実装は `src/avatar/look.ts`

---

## 3. 絵と、その描き方（決定済み）

**結論だけ先に**: 3D（react-three-fiber）で、モデルファイルを使わず
**球や円錐をコードで組んだひよこ**を描いています。以下は、そこに至った理由の記録です。

### 3-1. どう変わるかを決める

やったこと：**ステージごとに「何が増えるか」を1行ずつ決めた**（2章の表）。

考える切り口の例:

| 変え方 | 実装コスト | 例 |
| --- | --- | --- |
| パーツを足す | 低 | 装備、小物、背景の飾り、称号バッジ |
| 色を変える | 低 | 服の色がステージごとにリッチになる |
| 大きさ・姿勢 | 中 | 背が伸びる、姿勢がよくなる |
| 形そのもの | 高 | 体型・輪郭を描き直す |

**成果物**: 2章の表（ステージごとに「何が増えるか」1行）。
これが決まったので、A はデータの形を、C は絵を、それぞれ独立して作れます。

### 3-2. 技術選定：何で描くか

3-1 で描きたいものが決まってから方式を選びました。順序が逆になると
「描きたい絵が描けない方式」を選んでしまいます。

| 方式 | 向いているもの | 利点 | 欠点 |
| --- | --- | --- | --- |
| **インライン SVG**（採用前の方式。いまは 3D が使えない時の予備） | パーツの足し引き・色替え | 出し分けが効き、追加ライブラリ不要 | 複雑な絵は手書きが大変 |
| **画像ファイル**（PNG/WebP） | 描き込んだイラスト | 絵の自由度が最大。描ける人がいるなら早い | 段階×活力の枚数ぶん要る |
| **SVG ファイル読込** | Figma / Illustrator で描く | ツールで描ける。拡大に強い | パーツ制御は書き出し方しだい |
| **CSS アニメーション** | 揺れ、点滅、ステージアップ演出 | 上のどれとも併用できる | 絵そのものは作れない |
| **Lottie / Rive 等** | 凝ったアニメーション | 動きが作り込める | **GUI ツールで作ったファイルが要る。描ける人が居ないと1枚も出せない** |
| **3D（react-three-fiber）** ← 採用 | パーツの足し引き・体格の変化・動き | パーツを足すだけでステージが作れる。モデルファイル無しでコードだけで完結する | three.js のぶん容量が増える（遅延読み込みで回避済み） |

### なぜ react-three-fiber にしたか

- **絵を描ける人を待たなくていい。** 一度 Rive（GUI で作った `.riv` を読む方式）を試しましたが、
  ファイルが用意できるまで何も動かせず止まりました。コードで形を組む方式ならその待ちが無い
- **「増えるもの」を足すだけでステージが作れる。** つばさ・とさか・冠は、それぞれ数行の追加で済む
- **枚数が要らない。** 画像方式だと ステージ7 × 活力5 = 最大35枚 描くことになる
- **2軸の掛け合わせが自然に出せる。** 形（ステージ）と色・姿勢（活力）は別々の値で決まるので、
  「ステージ6でボロボロ」のような組み合わせも自動的に成立する

容量だけが欠点です（three.js は gzip で 250KB ほど）。アバターの 3D だけ別チャンクに分けて
遅延読み込みし、読み込みが終わるまでは従来の SVG を出すようにしてあります。

### 3-3. プロトタイプで確かめた結果

- [x] ステージ2〜6も同じ手間で作れるか → パーツを1つ足すだけ。1段階あたり数行
- [x] **活力レベルとの掛け合わせが表現できるか** → できる。形と色・姿勢を別の値で持っているため
- [x] A が用意するデータだけで絵が決まるか → 決まる。入口は
      `<Avatar lv={...} variant={...} vitality={...} days={...} />` だけ

---

## 4. タスクの割り振り

育成機能は3人全員に関わりますが、**触るファイルは分かれます。**

```text
     ┌─ ステップ0：絵と技術方式を決める（3章）← 済
     │
     ▼
   A：データを作る（logic.ts）      ← ここが先。型が決まらないと B・C が動けない
     │
     ├──▶ B：画面と演出（App.tsx）
     └──▶ C：絵を描く（src/avatar/）              ← B と C は並行でOK
```

### A：ロジック係 — `src/logic.ts`

`STAGES` / `stageOf()` / `daysToNextStage()` は、C が先に `src/avatar/stage.ts` へ
置いてあります（`logic.ts` は触っていません）。ステージを State に持たせたくなったら
そのまま移してください。

- [ ] `State` に累計の達成日数を持たせる（いまは `done.length` で代用中）
- [x] `STAGES` 定数と `stageOf(days)` 関数（`src/avatar/stage.ts`）
- [x] 「次のステージまであと何日か」を返す関数（`daysToNextStage()`）
- [ ] `stageOf()` のテストを書く（境界値：0日/1日、59日/60日）
- [ ] 既存の保存データを壊さないか確認（`STORAGE_KEY` を `v4` に上げるかの判断）

ステージの判定は `src/avatar/stage.ts` で先に動いているので、B と C はここを待たずに進められます。
`logic.ts` 側へ移すときは、関数名と型を変えないでください。

### B：画面係 — `src/App.tsx`

- [ ] 今のステージ名を画面に出す（`stageOf(state.done.length).name`）
- [ ] 「次のステージまであと N 日」の進捗表示
- [ ] **ステージが上がった瞬間の演出** ← 育成アプリで一番おいしい所。ここに時間を使う価値あり
- [ ] 「見た目をためす」スライダーを、ステージも動かせるように拡張（C の確認作業が楽になります）

### C：見た目係 — `src/avatar/` / `src/ui/styles.css`

触り方は [`src/avatar/README.md`](./src/avatar/README.md) にまとめてあります。

- [x] 3章で決めた絵を、ステージごとに実装（ひよこ7段階）
- [x] 2軸対応（ステージ＝形、活力＝色・表情・姿勢）
- [x] ステージが上がった瞬間に「ぽん」と跳ねる演出
- [ ] ステージ7段階 × 活力5段階 を一通り見て、破綻がないか確認 ← **ここが次にやること**
- [x] アバター3種（もりお／だいち／こむぎ）の色分け
- [ ] ひよこの造形の作り込み（`Chick.tsx` の数値調整。いくらでも良くできる）
- [ ] 種類ごとに色だけでなく形も変える（いまは色相だけが違う）

---

## 5. やらないこと（今は）

育成に集中するため、以下は後回しにします。

- 他人のアバターを見る／比べる機能
- アイテム、ガチャ、通貨などの追加システム
- サーバーへの保存（構成は [INFRA.md](./INFRA.md) で決めてあります。着手は育成が固まってから）

**理由**: まず「1人で続けたくなるか」が成立しないと、その先を足しても効きません。
ステージ6（まんまるの主）まで自分で育ててみて、面白いと思えたら次を考える。
