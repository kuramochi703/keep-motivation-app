# チーム開発の進め方

3人で `keep-motivation-app` を開発するための、分担・タスク・ルールをまとめたドキュメント。
環境構築は [README.md](./README.md)、育成システムの設計は [AVATAR.md](./AVATAR.md)、
サーバー・DB の構成は [INFRA.md](./INFRA.md) を参照。

> **このドキュメントは叩き台です。** 初期タスクは「プロトタイプに足りていないもの」から仮に組んだものなので、
> 作りたい機能や締め切りが決まっている場合は 3人で書き換えてから始めてください。

---

## 1. 分担の考え方：ファイルで分ける

3人が同じファイルを同時に触ると、必ずコンフリクト（Git の衝突）が起きます。
初心者チームで一番効くのは、**担当ファイルを分けてしまう**ことです。

| 担当 | 主なファイル | 役割 |
|---|---|---|
| **A：ロジック係** | `src/logic.ts` | 活力の増減ルール、連続日数、保存/読込 |
| **B：画面係** | `src/App.tsx` | 画面の構成、ボタン・タイマーの動き |
| **C：見た目係** | `src/styles.css`, `src/Avatar.tsx` | 配色・レイアウト、アバターの絵 |

**得意/不得意ではなく、まずランダムでも構いません。** 数週間ごとに交代すると全員が全体を触れます。

### 唯一の「接点」に注意

3人の作業がぶつかる場所は **`logic.ts` が公開している型と関数** だけです。

```
logic.ts  ──[ State型 / levelOf() / streak() など ]──▶  App.tsx  ──▶  Avatar.tsx / styles.css
```

**A が関数名や型を変えると B が壊れます。** 変える時は必ず先にチームへ共有してください（後述の「相談が必要な変更」）。

---

## 2. 初期タスク

### ステップ0：全員 — 慣らし運転（最初の1〜2日）

まず「PR を出してマージする」流れを全員が1回ずつ体験します。**中身は本当に小さくて構いません。**

- [ ] 全員: README の手順で `npm run dev` が動くところまで確認
- [ ] A: `logic.ts` の `GAIN` を 12 → 15 に変えて PR（動作確認したら閉じてOK、マージしなくてよい）
- [ ] B: `App.tsx` のヘッダー文言をどれか1つ書き換えて PR
- [ ] C: `styles.css` の `--accent` の色を変えて PR
- [ ] 全員: 他の2人の PR を1回ずつレビュー（「見ました」のコメントだけでも可）

**ここでの目的は機能ではなく、`git switch -c` → `push` → PR → レビュー → マージの流れを体に入れることです。**

### ステップ1：アバター育成をつくる（メイン）

**このアプリの中心はアバターの育成です。** 設計とタスクは [AVATAR.md](./AVATAR.md) に切り出しました。
ステップ0が終わったら、そちらへ進んでください。

3人全員が関わりますが、触るファイルは分かれます（A→データ、B→演出、C→絵）。
**まず3人で「どう育つか」の絵を決める**ところから始めます。

### ステップ2：育成に必要な足回り（並行 or 後）

育成そのものではないが、進めるうえで効いてくるもの。手が空いた時に。

- [ ] A: テストを導入する（`npm i -D vitest`）— `streak()` と `rollover()` は手で確認しづらく、一番バグりやすい
- [ ] B: タイマーの残り時間が、ページを閉じると 5:00 に戻ってしまう問題を直す
- [ ] C: スマホ幅（375px）で崩れていないか確認して直す

### ステップ3：その後の候補

アバターをステージ5まで自分で育ててみて、面白いと思えてから選び直してください。

- デプロイ（まず localStorage のまま公開してしまう）
- 記録を Supabase に保存する（他のPCからも見られるようにする）
- ログイン
- 3人分のアバターを見せ合う機能
- 通知・リマインダー

構成と手順は [INFRA.md](./INFRA.md) にまとめました。**この順番で進めてください**（一度に変えると原因が切り分けられなくなります）。

---

## 3. 週の進め方

```
月曜  15分の作戦会議：今週やることを1人1つ決める → GitHub の Issue にする
毎日  各自ブランチで作業 → 区切りがついたら PR
随時  PR が出たら、他の2人のどちらかが見る（1人でOK）
金曜  15分のふりかえり：できたこと / 詰まったこと / 来週やること
```

**会議は短く。** 進捗共有は口頭より PR と Issue に書いたほうが、あとから読めて楽です。

### 1人1タスクの原則

同時に複数のことをやらない。1つのブランチでは1つのことだけ。
理由：PR が小さいほどレビューしやすく、壊れた時に戻しやすいからです。

---

## 4. Git の運用ルール

```bash
git switch main && git pull        # ① 必ず最新から始める
git switch -c feat/timer-resume    # ② ブランチを作る
# ...編集...
npm run build                      # ③ 型エラーがないか確認
git add -A && git commit -m "タイマーの残り時間を保存するようにした"
git push -u origin feat/timer-resume
# ④ GitHub で PR を作る → レビューしてもらう → マージ
```

**ブランチ名の付け方**

| 種類 | 例 |
|---|---|
| 機能追加 | `feat/multiple-goals` |
| バグ修正 | `fix/streak-off-by-one` |
| 見た目 | `style/mobile-layout` |
| その他 | `chore/add-vitest` |

**守ること**
- `main` に直接 push しない（全員の作業が止まる事故につながります）
- PR は最低1人のレビューを通してからマージ
- `npm run build` が通らないものは PR にしない

---

## 5. 相談が必要な変更

以下は**自分の担当ファイルの中でも、勝手に変えない**でください。他の2人が壊れます。

- `logic.ts` の `State` 型（保存データの形）を変える
- `logic.ts` の関数名・引数を変える／削除する
- `STORAGE_KEY` を変える（※変えると全員の記録が消えます。変える時は `v3` → `v4` のように上げる）
- `package.json` にライブラリを足す
- `styles.css` の `:root` にある CSS 変数（`--accent` など）の名前を変える

**やり方**: チャットで「〇〇を変えたい、理由は△△」と投げて、2人が反応してから着手。

---

## 6. 詰まったときのルール

**15分ルール：15分考えてわからなかったら、必ず人に聞く。**

初心者3人だと「聞くのが申し訳ない」で全員が黙って止まるのが一番の失敗パターンです。
聞くときは以下をセットで出すと、相手も答えやすくなります。

1. 何をしようとしたか
2. 何が起きたか（**エラーメッセージは全文コピペ**、スクショより文字）
3. 何を試したか

---

## 7. 用語のミニ辞典

| 言葉 | 意味 |
|---|---|
| ブランチ | 作業用の分岐。他の人に影響を与えずに編集できる |
| PR（プルリク） | 「この変更を main に入れていい？」という申請 |
| コンフリクト | 同じ行を2人が別々に変えた時に起きる衝突。手で選んで直す |
| マージ | ブランチの変更を main に取り込むこと |
| HMR | 保存した瞬間にブラウザへ反映される仕組み |
| リファクタリング | 動きを変えずにコードを整理すること |
