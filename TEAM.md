# チーム開発の進め方

3人で `keep-motivation-app` を開発するための、**分担・タスク・ルール**。
アプリの機能と動かし方は [README.md](./README.md)、構成とデータの持ち方は
[ARCHITECTURE.md](./ARCHITECTURE.md) を参照。

> **このドキュメントは叩き台です。** タスクは「いま足りていないもの」から仮に組んだものなので、
> 作りたい機能や締め切りが決まっている場合は3人で書き換えてから始めてください。

---

## 1. 分担：ファイルで分ける

3人が同じファイルを同時に触ると、必ずコンフリクトが起きます。
初心者チームで一番効くのは、**担当ファイルを分けてしまう**ことです。

| 担当 | 主なファイル | 役割 |
| --- | --- | --- |
| **A：ロジック係** | `src/state/` | 活力の増減ルール、連続日数、Supabase の読み書き |
| **B：画面係** | `src/app/` `src/pages/` `src/features/` | 画面の構成、ボタン・タイマーの動き |
| **C：見た目係** | `src/ui/` `src/avatar/` | 配色・レイアウト、アバター（[触り方](./src/avatar/README.md)） |

**得意/不得意ではなく、まずランダムでも構いません。** 数週間ごとに交代すると全員が全体を触れます。
（「この作業はどのファイル？」は [README 4章](./README.md#4-どこを触ればいいか)）

### 唯一の「接点」に注意

3人の作業がぶつかる場所は **`state/logic.ts` が公開している型と関数** だけです。

```text
logic.ts ──[ State型 / levelOf() / streak() など ]──▶ pages/ ──▶ avatar/ , ui/
```

**A が関数名や型を変えると B・C が壊れます。** 変える時は必ず先に共有してください（6章）。

---

## 2. ルールは「必要を感じてから」入れる

最初から PR・レビュー・Issue を全部揃えると、**まだ何も無いのに手続きだけが重い**状態になります。
**痛みが出てから、その痛みに効くものを入れる**方針で進めます。

| 仕組み | 今は | 入れる合図（これが起きたら導入） |
| --- | --- | --- |
| feature ブランチ | **入れる** | — （無いと3人が同じ場所を壊し合います） |
| PR・レビュー | **入れる** | — （[PRテンプレ](./.github/PULL_REQUEST_TEMPLATE.md) あり） |
| `npm run build` を通してから共有 | **入れる** | — （5章の「守ること」） |
| Issue / チケット | まだ | 「誰が何をやってるか分からない」「同じ所を2人が触った」が起きたら |
| テスト（vitest） | まだ | 同じバグを2回直したら |
| lint / format | まだ | スタイルの指摘がレビューで出るようになったら |
| 自動デプロイ | まだ | 手動デプロイが面倒になったら |

**入れると決めたら、この表を「入れる」に書き換えてください。**
ドキュメントと実際の運用がずれるのが一番たちが悪いです。

> **例外は1つだけ：`main` に直接 push しない。**
> これは手続きではなく事故防止です。壊れた `main` は3人全員の作業を止めます。

---

## 3. いまのタスク

### 育成まわり（メイン）

このアプリの**中心はアバターの育成**です。タイマーも記録も、育成を支えるための部品です。
3人全員が関わりますが、触るファイルは分かれます。

**A：ロジック係** — `src/state/`

- [ ] `State` に累計の達成日数を持たせる（いまは `done.length` で代用中）
- [ ] `stageOf()` のテストを書く（境界値：0日/1日、59日/60日）
- [x] `STAGES` / `stageOf()` / `daysToNextStage()`（C が `src/avatar/stage.ts` に置いた。
      `state/` へ移したくなったら**関数名と型を変えずに**そのまま移す）

**B：画面係** — `src/app/` `src/pages/`

- [ ] 今のステージ名を画面に出す（`stageOf(state.done.length).name`）
- [ ] 「次のステージまであと N 日」の進捗表示
- [ ] **ステージが上がった瞬間の演出** ← 育成アプリで一番おいしい所。時間を使う価値あり
- [ ] 「見た目をためす」スライダーを、ステージも動かせるように拡張（C の確認が楽になる）

> 初期プロトタイプ（`docs/prototype.html`、削除済み）には **「最低ライン」「のべ日数」「この5週間」**
> の表示がありました。デザインの参考にしたい時は git の履歴から取り出せます:
> `git log --all --oneline -- docs/prototype.html` でファイルがあった頃のコミットを探し、
> `git show <コミットID>:docs/prototype.html > /tmp/prototype.html` でブラウザで開けます。

**C：見た目係** — `src/avatar/` `src/ui/`

- [x] ステージ6段階 ＋ たまごの実装、2軸対応、進化の「ぽん」演出、アバター3種の色分け
- [x] Blender のモデル（`models/chick.blend`）に差し替え
- [ ] **とさかを常に出すかどうかを決める** ← 設計図では基本パーツだが、
      いまはステージ4（14日）まで隠している。常時表示にするならステージ4の
      「増えるもの」を別のものに差し替える必要がある（要相談）
- [ ] ステージ6段階 × 活力5段階 を一通り見て、破綻がないか確認
- [ ] 種類ごとに色だけでなく形も変える（いまは色相だけが違う）

### 足回り（手が空いた時に）

内容の詳細は [ARCHITECTURE 5章 既知の制約・課題](./ARCHITECTURE.md#5-既知の制約課題)。ここは担当と着手状況だけ持ちます。

- [ ] A: テストを導入する（`npm i -D vitest`）— `rollover()` と `streak()` が一番バグりやすい
- [ ] A/B: lint / format を入れる（`@typescript-eslint` + `eslint-plugin-react-hooks` で十分）
- [ ] A: `lastDate` を DB に保存する（再読み込みをまたぐと活力減少がずれる）
- [ ] B: タイマーの残り時間が、ページを閉じると 5:00 に戻ってしまう問題を直す
- [ ] C: スマホ幅（375px）で崩れていないか確認して直す
- [ ] 認証と RLS を入れる ← **いまは誰が開いても同じデータ。公開するなら必須**

### その後の候補

アバターをステージ6まで自分で育ててみて、面白いと思えてから選び直してください。

- デプロイ（[ARCHITECTURE 6章](./ARCHITECTURE.md#6-どこに置くかデプロイ)）
- ログイン
- 3人分のアバターを見せ合う機能
- 通知・リマインダー

### やらないこと（今は）

- 他人のアバターを見る／比べる機能
- アイテム、ガチャ、通貨などの追加システム

**理由**: まず「1人で続けたくなるか」が成立しないと、その先を足しても効きません。

---

## 4. 週の進め方

```text
月曜  15分の作戦会議：今週やることを1人1つ決める
毎日  各自ブランチで作業 → 区切りがついたら PR
随時  PR が出たら、他の2人のどちらかが見る（1人でOK）
金曜  15分のふりかえり：できたこと / 詰まったこと / 来週やること
        ＋「今週めんどくさかったこと」を1人1つ → 仕組みを足すか決める（2章の表を更新）
```

**会議は短く。** 進捗共有は口頭より PR に書いたほうが、あとから読めて楽です。

**1人1タスクの原則**: 同時に複数のことをやらない。1つのブランチでは1つのことだけ。
**PR が小さいほどレビューしやすく、壊れた時に戻しやすい**からです。

---

## 5. Git の運用ルール

```bash
git switch main && git pull        # ① 必ず最新から始める
git switch -c feat/timer-resume    # ② ブランチを作る
# ...編集...
npm run build                      # ③ 型エラーがないか確認
git add -A && git commit -m "タイマーの残り時間を保存するようにした"
git push -u origin feat/timer-resume
# ④ GitHub で PR を作る → レビューしてもらう → マージ
```

### ブランチ名の付け方

| 種類 | 例 |
| --- | --- |
| 機能追加 | `feat/multiple-goals` |
| バグ修正 | `fix/streak-off-by-one` |
| 見た目 | `style/mobile-layout` |
| ドキュメント | `docs/consolidate` |
| その他 | `chore/add-vitest` |

### 守ること

- **`main` に直接 push しない**（全員の作業が止まる事故につながります）
- PR は最低1人のレビューを通してからマージ
- `npm run build` が通らないものは PR にしない

---

## 6. 相談が必要な変更

以下は**自分の担当ファイルの中でも、勝手に変えない**でください。他の2人が壊れます。

- `state/logic.ts` の `State` 型（保存データの形）を変える
- `state/logic.ts` の関数名・引数を変える／削除する
- Supabase のテーブル定義・カラム名を変える（**全員のデータが読めなくなります**）
- `package.json` にライブラリを足す
- `ui/styles.css` の `:root` にある CSS 変数（`--accent` など）の名前を変える
- `avatar/` のマテリアル名・オブジェクト名を Blender 側で変える
  （**コードが名前で色を当てているので、黙って色が付かなくなります**）

**やり方**: チャットで「〇〇を変えたい、理由は△△」と投げて、2人が反応してから着手。

---

## 7. 詰まったときのルール

**15分ルール：15分考えてわからなかったら、必ず人に聞く。**

初心者3人だと「聞くのが申し訳ない」で全員が黙って止まるのが一番の失敗パターンです。
聞くときは以下をセットで出すと、相手も答えやすくなります。

1. 何をしようとしたか
2. 何が起きたか（**エラーメッセージは全文コピペ**。スクショより文字）
3. 何を試したか
